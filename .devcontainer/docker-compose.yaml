services:
  server:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ..:/workspace
    working_dir: /workspace
    ports:
      - "8000:8000"
    tty: true